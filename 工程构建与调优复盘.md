# CUDA 算子工程构建与调优复盘

## 1. 目标与边界

本项目的目标不是编写一次性的 CUDA demo，而是建立一个可持续迭代的算子研发骨架：算子实现可被下游项目稳定链接，
正确性、性能、调优与性能分析均有可复现入口，并能在 CI 和真实 GPU 上执行。

首个落地算子选择 Transformer 热路径中的 **fused RMSNorm forward**：

```text
y = RMSNorm(x + residual) * gamma
```

该路径融合了可选残差加法、平方和归约、inverse RMS 计算与逐元素缩放，避免了中间张量的全局内存读写，适合作为
带宽受限 CUDA 算子优化的起点。

## 2. 工程设计

工程按“稳定接口—内核实现—验证与运行工具”分层：

| 层次 | 路径 | 设计要点 |
| --- | --- | --- |
| 公共接口 | `include/cuda_ops/` | 框架无关的 `TensorView`、dtype、状态码和 CUDA 错误检查；避免将核心库锁定到单一框架。 |
| CUDA 实现 | `src/rmsnorm.cu` | 按 dtype、是否有 residual、是否保存 `inv_rms` 做编译期分派，保持热路径无运行时分支。 |
| 测试 | `tests/` | 基于 CPU reference 验证 FP16/FP32、残差、大维度和带 padding 的 row stride。 |
| 基准 | `benchmarks/` | 使用 CUDA Event 计时，输出机器可读 JSON，报告延迟与按最小读写流量估算的有效带宽。 |
| 调优/分析 | `tools/`、`scripts/` | 参数扫频自动调优与 Nsight Compute 分析入口。 |
| 发布接入 | `CMakeLists.txt`、`cmake/`、`examples/consumer/` | 可安装 CMake package、下游 `find_package` 示例、静态 CUDA 库的最终 device link 说明。 |
| 自动化 | `.github/workflows/`、`Dockerfile` | 普通 CI 负责编译，具备 GPU 的 self-hosted runner 执行正确性与性能流程。 |

### 内核实现策略

每个 block 处理一行 hidden states。线程以 stride 方式遍历该行，先在寄存器中累积平方和；归约采用 warp shuffle，
仅将每个 warp 的局部结果放入 shared memory。线程 0 计算 inverse RMS，随后所有线程完成缩放和写回。

该设计在常见 Transformer hidden size（例如 768、1024、4096、8192）上避免了额外 kernel launch 和中间 buffer，
并且通过 `row_stride` 支持带 padding 的行主序张量。

## 3. 工具链准备

实际环境为 Windows、CUDA 13.3、RTX 3060（SM 8.6）和 Visual Studio Build Tools。开始时 CMake、Ninja 与可直接
使用的 MSVC 环境没有在 shell 中就绪，因而完成了以下准备：

1. 安装 CMake 4.4.3 与 Ninja 1.13。
2. 确认本机已有 MSVC 19.51 与 CUDA 13.3，并在 `tools/devshell.cmd` 中通过 `vswhere` 自动定位 Build Tools。
3. 通过该脚本激活 `cl.exe`、CMake 和 Ninja，使 Windows 上的 `nvcc` 主机编译阶段可复现。
4. 将构建目标架构固定为当前机器的 `86`，避免生成无关 fatbin。

建议在 Windows 中使用：

```bat
cmd /k tools\devshell.cmd
```

然后执行配置、构建、测试：

```bat
cmake -S . -B build\release -G Ninja -DCMAKE_BUILD_TYPE=Release -DCUDA_OPS_CUDA_ARCHITECTURES=86
cmake --build build\release --parallel
ctest --test-dir build\release --output-on-failure
```

## 4. 开发与验证过程

### 4.1 功能契约

`rmsNormForward` 对输入、可选 residual、weight 和 output 做显式检查：

- dtype 仅支持 FP16、FP32，且参与计算的张量 dtype 必须一致；
- 输入/输出形状为二维，weight 形状为 `[1, cols]`；
- row stride 以元素为单位，允许大于等于 `cols`；
- epsilon 必须为有限正数；
- `store_inv_rms=true` 时，调用者必须提供长度为 `rows` 的 `float` device buffer；
- launch 与给定 CUDA stream 有序，但对 host 保持异步。

### 4.2 正确性结果

Release 构建的 CTest 已通过。测试使用 CPU 参考实现，对比最大绝对误差：

| 用例 | 最大绝对误差 |
| --- | ---: |
| FP32，17×768，无 residual | 5.96e-7 |
| FP32，8×4097，有 residual | 1.43e-6 |
| FP16，31×1024，无 residual | 4.89e-4 |
| FP16，9×8192，有 residual | 9.68e-4 |
| FP16，11×777，stride=1024，有 residual | 9.34e-4 |

误差均在测试设定的容差内，且覆盖了无法被 block size 整除的行长度与非紧凑行间距。

### 4.3 发布包验证中发现并修复的问题

构建核心工程本身通过并不等于“可接入”。安装并构建独立 consumer 时发现了以下问题，并已修复：

| 问题 | 原因 | 修复 |
| --- | --- | --- |
| 导出 CMake target 失败 | `INTERFACE_INCLUDE_DIRECTORIES` 使用源码绝对路径 | 使用 `BUILD_INTERFACE` / `INSTALL_INTERFACE` 分离构建与安装路径。 |
| consumer 找不到 CUDA runtime target | package config 未声明 CUDA 依赖 | 使用 `find_dependency(CUDAToolkit)`。 |
| consumer 无法识别 `std::string_view` | 核心 target 的 C++20 要求未传播 | 导出 `target_compile_features(cuda_ops PUBLIC cxx_std_20)`。 |
| 静态库 device symbol 未解析 | 下游最终可执行文件没有 device link | consumer 启用 CUDA language 与 `CUDA_RESOLVE_DEVICE_SYMBOLS ON`。 |
| Release 库与 Debug consumer 不兼容 | MSVC runtime/iterator debug level 不匹配 | 示例明确要求 consumer 使用与库一致的 Release 配置。 |

最终，安装包已能通过 `find_package(cuda_ops CONFIG REQUIRED)` 被 `examples/consumer` 独立配置、构建并执行。

## 5. 性能调优过程

### 5.1 测量原则

RMSNorm 融合核通常是带宽受限的。基准使用 CUDA Event 测量 kernel 平均执行时间，并按以下最小数据流量估算有效带宽：

- 无 residual：读 input、读 gamma、写 output；
- 有 residual：额外读 residual。

`effective_gbps` 用于同一 GPU、相同 shape/dtype/CUDA 版本条件下的回归对比，不应直接与显存理论峰值等同。

### 5.2 自动调优

`tools/autotune.py` 分别以 128、256、512 threads-per-block 构建并压测候选内核。实际负载为：

- GPU：NVIDIA GeForce RTX 3060（SM 8.6）
- dtype：FP16
- shape：4096×4096
- residual：启用
- iterations：200

结果如下：

| Threads/block | 平均延迟（µs） | 有效带宽（GB/s） |
| ---: | ---: | ---: |
| 128 | 469.970 | 285.588 |
| 256 | 342.364 | 392.032 |
| 512 | **304.845** | **440.282** |

因此本机默认 `CUDA_OPS_RMSNORM_THREADS` 已设为 **512**。复测 300 次迭代时得到 304.749 µs、440.420 GB/s，
与调优结果一致。原始结果保存于 `build/autotune/rmsnorm_tuning.json`。

### 5.3 调优结论的适用范围

512 threads 是该 GPU、该工作负载上的结果，不应直接推广到其他架构、hidden size 或精度。新增部署目标时应：

1. 设置对应 `CUDA_OPS_CUDA_ARCHITECTURES`；
2. 使用真实 shape bucket、dtype、residual 模式运行自动调优；
3. 记录 JSON 结果，并为关键 workload 建立性能回归阈值；
4. 使用 Nsight Compute 判断瓶颈是内存吞吐、occupancy、寄存器压力还是指令效率后再修改内核。

## 6. 当前可用流程与限制

当前可执行的工程闭环是：

```text
激活工具链 → CMake 配置/构建 → CTest 正确性 → 基准 JSON → 自动调优 → 安装包 → 独立 consumer 验证 → Nsight Compute 分析
```

其中构建、正确性、基准、自动调优和安装包 consumer 已在当前环境实际验证。

有两项环境级限制尚需管理员处理：

1. **Device Guard**：会拦截部分刚生成的 benchmark 可执行文件。已存在并被策略信任的调优产物可以运行；长期方案是让管理员将项目构建输出纳入信任策略，而不是尝试绕过策略。
2. **GPU Performance Counters 权限**：Nsight Compute 已安装并可以启动目标进程，但硬件计数器采集返回 `ERR_NVGPUCTRPERM`。需由设备管理员启用相应权限，之后可使用 `scripts/profile.ps1` 生成 `.ncu-rep` 分析报告。

## 7. 后续演进建议

1. 增加 BF16、vectorized `half2` 与针对短行/长行的独立 dispatch 路径。
2. 实现 backward，并以 PyTorch 或推理引擎适配层接入模型运行时；核心 `TensorView` API 保持不变。
3. 增加随机 shape/property tests、CUDA Compute Sanitizer 流程和性能回归基线。
4. 在具备 GPU 的 CI runner 上启用 CTest、基准 JSON 对比与 Nsight Compute report 归档。
5. 若下游要求最简链接，可提供已完成 device link 的 shared library 发行形式；当前 static library 方案要求最终 consumer 启用 CUDA device link。

